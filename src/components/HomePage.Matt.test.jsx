import { it } from 'vitest';
import { render, screen } from '@testing-library/react';
import App from "../App" 

describe("Check budget buddy image", () => {
    it("When the expenses exceed budget, then the logo image should load “angry version” of BudgetBuddy icon", async () => {

    }); 
}); 